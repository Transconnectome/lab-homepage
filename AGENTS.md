# Connectome Lab 홈페이지 작업 지침

## 먼저 확인할 사항

- `README.md`, `claudedocs/HANDOFF.md`, 작업 경로에 적용되는 추가 지침과 `git status`를 확인한다. 문서의 과거 상태 설명은 현재 코드와 대조한다.
- 기존 사용자 변경사항을 덮어쓰거나 되돌리지 않는다. 관련 없는 파일은 수정하지 않는다.
- 기존 Astro 정적 사이트, React 컴포넌트, Tailwind, 콘텐츠 컬렉션 구조를 유지한다. 한국어는 루트, 영어는 `/en/`이며 기존 리디렉션과 언어별 URL을 보존한다.
- 작업 보고는 한국어로 한다. 공개 페이지는 기존 언어별 의미와 불확실성 수준을 일치시킨다.

## 연구 소개의 작성·수정·검수

연구 설명이나 과학적 주장에 영향을 주는 작업을 시작하기 전에 반드시 `docs/research-editorial-guidelines.md`를 처음부터 끝까지 읽는다. 반복 작업 절차는 `.agents/skills/connectome-research-editor/SKILL.md`에 있다. 상세 지침은 한 곳에서 관리하고 다른 문서에 복제하지 않는다.

- 기술 목록이 아니라 **연구 질문, 지식의 빈틈, 방법 선택의 이유, 실제 기여, 한계**를 설명한다.
- 핵심 주장을 확인한 원자료의 해당 절·그림·표와 연결한다. 제목·초록·검색 요약만 확인하고 원문 전체를 검토했다고 표현하지 않는다.
- 관찰·예측·인과·기제·임상 유용성을 구분하고, 결과·해석·진행 중 연구·포부를 혼합하지 않는다.
- 조사와 과학적 검토를 먼저 하고 그다음 화면에 반영한다. 짧은 소개·개요·상세 설명을 연결하여 깊이와 가독성을 함께 확보한다.
- 서브에이전트는 실제 사용할 수 있을 때만 최대 두 명을 읽기 중심 조사·비판적 검토에 배정한다. 사이트 파일은 메인 에이전트만 수정한다.
- 과학적 의미를 바꾸지 않는 오탈자·간격·색상 수정이나 지침 설치만 요청받은 경우에는 전체 연구 조사를 시작하거나 연구 본문까지 임의로 수정하지 않는다.

## 검증과 변경 범위

- 현재 `package.json`에서 제공하는 명령을 확인한다. 기본 개발 명령은 `npm ci`, `npm run dev`, `npm run build`, `npm run preview`다. 존재하지 않는 lint/test 명령을 실행했다고 주장하지 않는다.
- 사이트 콘텐츠·코드 변경은 빌드를 실행하고, 사용 가능한 브라우저 도구로 모바일·데스크톱의 읽기 흐름, 언어별 링크, 키보드 접근성을 확인한다.
- 지침 문서만 변경한 경우에는 참조 경로·스킬 메타데이터·변경 파일 범위를 검증하고, 사이트 빌드나 브라우저 검증을 생략했다면 이를 명시한다.
- 과학적 검토와 UI·빌드 검증은 별개다. 실행하지 못한 검증은 통과로 처리하지 않는다. 자동 생성 파이프라인이 검수된 본문을 덮어쓰는지도 확인한다.
- 비공개 자료, 개인정보, API 키와 비밀값을 홈페이지·공개 저장소·PR·검토 기록에 올리지 않는다. 읽을 수 있다는 사실을 공개 승인으로 취급하지 않는다.
- 원격 쓰기는 사용자가 승인한 범위에서만 수행한다. `main` push는 배포를 유발하므로 운영 배포 승인이 없으면 별도 브랜치와 PR을 사용하고 병합하지 않는다. 워크플로·도메인·유료 서비스 설정을 임의로 바꾸지 않는다.

## 완료 보고

지침 등록, 실제 연구 본문 수정, 검증, 운영 배포 상태를 각각 구분한다. 변경 파일, 실제 수행한 검증, 수정 전후 예시와 남은 판단 사항을 보고한다. 내용 개편 작업에서는 공개 가능한 주장–출처 대응과 검토 결과를 `docs/research-content-review.md`에 남긴다. 설정 파일 생성만으로 연구 소개 개편이 끝났다고 보고하지 않는다.
