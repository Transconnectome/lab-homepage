---
import Navbar from '../components/common/Navbar.astro';
import Footer from '../components/common/Footer.astro';
import AskLabAI from '../components/ai/AskLabAI';
import type { Lang } from '../i18n/ui';
import { altLangPath } from '../i18n/ui';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  lang?: Lang;
}

const {
  lang = 'ko',
  title = lang === 'ko'
    ? '서울대학교 커넥톰 연구실 | SNU Connectome Laboratory'
    : 'SNU Connectome Laboratory | 서울대학교 커넥톰 연구실',
  description = lang === 'ko'
    ? '서울대학교 커넥톰 연구실(PI 차지욱). 뇌의 연결이 어떻게 마음을 만들어내는지 연구합니다 — 뇌 파운데이션 모델(Neuro-X, NeuroMamba, DIVER-0), 멀티모달 유전체학, 계산정신의학, 양자 머신러닝.'
    : 'Seoul National University Connectome Lab (PI: Jiook Cha). We study how the brain\'s connections give rise to mind — with brain foundation models (Neuro-X, NeuroMamba, DIVER-0), multimodal genetics, computational psychiatry, and quantum machine learning.',
  ogImage = '/assets/og-image.jpg',
} = Astro.props;

// Korean is the default locale at the root; English lives under /en/.
const here = new URL(Astro.request.url).pathname;
const altPath = altLangPath(here, lang);
const canonical = new URL(here, Astro.site).toString();
const altHref = new URL(altPath, Astro.site).toString();
const koHref = lang === 'ko' ? canonical : altHref;
const enHref = lang === 'en' ? canonical : altHref;
---

<!doctype html>
<html lang={lang} class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />

    <!-- SEO Meta Tags -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="alternate" hreflang="ko" href={koHref} />
    <link rel="alternate" hreflang="en" href={enHref} />
    <link rel="alternate" hreflang="x-default" href={koHref} />
    <meta name="keywords" content="Connectome Lab, Jiook Cha, 차지욱, Seoul National University, SNU, Large Brain Model, Neuro-X, NeuroMamba, DIVER-0, SwiFT, fMRI, EEG, Quantum Machine Learning, Polygenic Risk Score, ABCD Study" />
    <meta name="author" content="Jiook Cha, SNU Connectome Lab" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={Astro.url} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={Astro.url} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={ogImage} />

    <!-- Fonts: Hahmlet (display serif, KR+Latin), IBM Plex Mono (captions/data).
         Only the weights actually used are requested. Korean webfonts ship one
         file per unicode subset per weight, so every unused weight multiplied
         the payload across ~13 Hangul subsets. -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Hahmlet:wght@600&family=IBM+Plex+Mono:wght@400;500&display=swap"
      rel="stylesheet"
    />
    <!-- Pretendard Variable (body, KR+Latin) -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css"
    />
  </head>
  <body id="top" class="min-h-screen flex flex-col bg-paper text-ink">
    <a
      href="#main"
      class="sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-4 focus:z-50 focus:rounded-lg focus:bg-white focus:px-4 focus:py-2 focus:text-sm focus:shadow-lg"
    >
      Skip to content
    </a>

    <Navbar lang={lang} />

    <main id="main" class="flex-1">
      <slot />
    </main>

    <!-- Floating lab guide (curated FAQ, not a live AI) -->
    <AskLabAI client:idle />

    <Footer lang={lang} />
  </body>
</html>
